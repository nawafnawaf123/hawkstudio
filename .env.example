# ======================
# Core
# ======================
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=CHANGE_ME_TO_A_LONG_RANDOM_SECRET

# SQLite default (local)
DATABASE_URL="file:./dev.db"

# Optional: lock admin email (leave empty to allow any user with role ADMIN)
ADMIN_EMAIL=admin@hawk.studio

# Contact phone shown on site
CONTACT_PHONE=+96170000000
CONTACT_WHATSAPP=+96170000000
CONTACT_EMAIL=hello@hawk.studio

# Optional SMTP to send contact emails
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM="Hawk Studio <no-reply@hawk.studio>"
